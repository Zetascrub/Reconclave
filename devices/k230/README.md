# T-Display K230

Planned role: capability-aware touch console plus edge perception node. The
target remains an interface boundary until hardware and its SDK are available;
portable protocol work does not depend on it.
